Name: WorkflowTest123
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    Branches:
      - branchupdate

# Required - Define action configurations.
Actions:
    Mock:
        Identifier: aws/workflows-mock-gamma@v1
        Inputs:
            Sources:
            - WorkflowSource